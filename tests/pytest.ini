[pytest]
testpaths = unit integration rag_quality
asyncio_mode = auto
addopts =
    -v
    --tb=short
    --strict-markers
markers =
    unit: Fast isolated unit tests (no DB or network)
    integration: Tests requiring a running database
    slow: Long-running tests (RAG quality, benchmarks)
    stress: Locust stress tests (run separately)
