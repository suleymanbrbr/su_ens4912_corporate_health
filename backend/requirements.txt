fastapi==0.111.0
uvicorn==0.30.0
python-jose[cryptography]==3.3.0
pydantic[email]==2.7.4
python-dotenv==1.0.1
# auth_utils uses the bcrypt package directly (passlib 1.7.4 is incompatible
# with bcrypt >=4.1 due to passlib's internal detect_wrap_bug probe).
bcrypt>=4.1.0,<5.0

# ── LangChain 0.3.x ecosystem (langchain-core 0.3.x ile uyumlu) ──────────────
langchain==0.3.25
langchain-community==0.3.24
langchain-huggingface==0.1.2
langchain-google-genai==2.0.7
langchain-openai==0.2.14
langchain-text-splitters==0.3.8
google-generativeai>=0.8.0

# ── Database & Vector ─────────────────────────────────────────────────────────
psycopg2-binary==2.9.9
pgvector==0.3.2

# ── ML / Embeddings ───────────────────────────────────────────────────────────
sentence-transformers==5.5.1
transformers==4.44.2
torch==2.3.1

# ── Document Processing ───────────────────────────────────────────────────────
pypandoc==1.13
python-docx==1.1.2
pypdf==4.2.0
numpy==1.26.4

# ── Multi-tenant SaaS: per-user encrypted API keys & live key validation ─────
cryptography>=42.0.0
httpx>=0.27.0
